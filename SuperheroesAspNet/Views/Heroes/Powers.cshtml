@model IEnumerable<string>

<h2>Superpowers of @ViewBag.SuperheroName</h2>

@if (!Model.Any())
{
    <p>No superpowers found for this hero.</p>
}
else
{
    <ul>
        @foreach (var power in Model)
        {
            <li>@power</li>
        }
    </ul>
}

<a asp-action="Index">Back to List</a>